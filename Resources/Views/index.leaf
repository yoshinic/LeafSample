<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>#(title)</title>
  </head>
  <body>
    <p>#(title)</p>
    #for(i in a):
      <p>#comma(i)</p>
    #endfor
  </body>
</html>